{
  "mcpServers": {
    "context7": {
      "command": "node",
      "args": ["-e", "require('https').get('https://mcp.context7.com/mcp', (res) => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => console.log(JSON.stringify({tools: [{name: 'context7', description: 'Project context and documentation server'}]}))); });"],
      "env": {
        "NODE_TLS_REJECT_UNAUTHORIZED": "0"
      }
    }
  }
}
